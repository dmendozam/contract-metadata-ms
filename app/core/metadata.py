

from app.schemas.schemas import Metadata


def process_metadata(request_date: str,
                     client_info: dict, 
                     metadata: Metadata):
    
    
    response = {"metadata": "a"}
    return response